import SwiftUI
import Kingfisher

struct MovieCell: View {
    
    let movie: Movie
    
    var body: some View {
        VStack {
            Text("Hello world!")
        }
        .padding(.bottom, 16)
    }
}
